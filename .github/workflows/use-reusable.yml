name: Test Script
on: 
    workflow_dispatch:
    push:
jobs:
    test:
        uses: ./.github/workflows/reusable.yml
    result:
        needs: test
        runs-on: ubuntu-latest
        steps:
            - name: Get result
              run: echo ${{ needs.test.outputs }}